# Collection of issues that needs to be addressed

## Camera/Viewport
 - Switching camera mode sets the camera pointing at the origo. It should keep the same origo or center as the previous camera mode.
 - Switching camera to room takes you to top down view. Changing tool again should remember your last camera preference. 
 - Skyboxes per environment

## UI
- Consolidate building tiles, rooms, environment, props into one tool
- Categorise props so they are easy to find + filter search
- Catogorise characters so they are easy to find + filter search

## General
- Scene starter, DM chooses environment for his scene: dungeon, forrest, village, crypt, etc
- Build a catalogue of default npcs and characters